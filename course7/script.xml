<speak version="1.0" xmlns="http://www.w3.org/2001/10/synthesis" xml:lang="zh-CN">
  <voice name="zh-CN-YunyiMultilingualNeural">
    <p>
      大家好，欢迎来到由IDEA研究院基础软件中心为大家带来的现代编程思想公开课。
      今天是第七节课，主题是命令式编程。
      <break time="500ms" />
    </p>
    <p>
      到目前为止，我们介绍的编程知识都可以归类在函数式编程的范畴。
      那么什么是函数式编程呢？
      函数式编程，顾名思义，类似于数学中的函数，对于每一个输入，总是有着特定的输出。
      对于每个标识符，我们可以直接用定义的值进行替代，这个性质被称为引用透明性。
      不过，为了开发一些实用的程序，我们通常会需要计算以外的一些副作用。
      举例来说，我们可能需要进行输入输出，我们可能要修改内存中的数据等。
      这些副作用就有可能会破坏我们的引用透明性，使得多次执行的结果不一致。
      <break time="500ms" />
    </p>
    <p>
      让我们先来看一个简单的例子，重新回顾一下我们之前学习的化简规则。
      我们定义如下的数据绑定和函数：
      一个值为 2 的 x，以及一个计算整数平方的 square 函数。
      之后，我们应用 square 函数对 eks 进行运算。
      我们可以简单地将 square 与 eks 分别用它们的定义替换，运算的结果事实上是一样的。
      这也是我们一直以来介绍的运算规则：将标识符用定义的数据进行替换。
      这样的引用透明性规则十分简单，十分易于理解。
      而我们接下来要看到的就不一定了。
      <break time="500ms" />
    </p>
    <p>
      我们首先来介绍一个简单的命令：函数 print line。
      在月兔中，print line 被用来输出一个字符串，并在结尾换行，例如 print line <lang xml:lang="en-US">Hello MoonBit!</lang> 就会输出字符串 <lang xml:lang="en-US">Hello MoonBit</lang>。
      月兔中，我们利用 init 代码块来定义初始化指令。
      这个代码块将会在程序的最初进行运算。
      到目前为止，可以简单地把它看成是程序的主入口，也就是程序会从这里开始执行。
      例如，我们将两者组合起来，使用 print line 作为输出。
      在我们的语言导览网页环境运行这段代码，可以看到它在下方输出了我们所想要显示的字符。
      <break time="500ms" />
    </p>
    <p>
      在这里，我们展示副作用对于程序理解的影响。
      我们在这里依然定义了我们的平方函数。
      与之前不同的是，我们的 eks 的定义变<phoneme alphabet="sapi" ph="wei 2">为</phoneme>了一个代码块，并且这个代码块中，除了 1 + 1 作为绑定的值，还有一个额外的命令，这个命令会输出 <lang xml:lang="en-US">hello moonbit</lang>。
      这个时候，我们不能简单地对标识符 eks 进行替换操作，而是要先对 eks 的定义进行求值，再用求得的值，替换 eks 出现过的地方。
      在这里，执行顺序从上到下。
      我们首先计算 eks 的值。计算 eks 的值的时候也是从上到下，先执行输出命令，因此我们在下方结果中可以看到一次输出。
      之后我们获得 2 作为整个代码块的值绑定到 eks 上。
      之后，再出现 eks 的时候，便会用 2 替代。
      例如之后的 z 的值，就会是 2 乘以 2 的结果，也就是 4。
      <break time="500ms" />
    </p>
    <p>
      而如果我们按照之前的那样，直接将所有 eks 和 square 出现的地方替换为定义的话，便会如代码所示。
      我们再来捋一下这一个部分。
      首先，我们计算乘号左右两侧代码块的值，最后将两者相乘。
      求乘号左侧代码块的时候，我们从上到下依次执行，进行了一次输出，获得了值 2。
      之后我们对乘号右侧代码块进行求值，进行了一次输出，获得了值 2。
      因此，我们在下方的运行结果中可以看到，此时的运行结果是两次输出，而非一次输出。
      因此我们可以看到，命令带来的副作用会破坏引用透明性，让代码的理解难度加大。
      <break time="500ms" />
    </p>
    <p>
      大家可能会好奇，命令有没有值，以及输出函数的值是什么。
      通常情况下，我们在执行带有副作用的命令后，可能不关心它的具体运行状况，那么这种时候我们一般会用单值类型 Unit 来表示。
      它也可以看成是长度为零的多元组。
      我们可以声明如下函数的返回值类型为 Unit。
      可以看到，let 语句本身也是有值的，这个值就是单值类型，它符合整个函数的类型声明，因此可以正常编译。
      <break time="500ms" />
    </p>
    <p>
      接下来我们要介绍的是变量。
      在月兔中，我们可以通过 let <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 在代码块中定义变量。
      我们通过 let <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 定义一个标识符，给它赋予最初的值。
      之后，我们可以通过 eks 等于 10 的形式来更换其中的值。
      赋值操作是一个命令，因此它的值也是单值类型 Unit。
      <break time="500ms" />
    </p>
    <p>
      我们之前介绍过结构体。
      在月兔中，结构体的字段默认是不可变的。
      不过，我们也允许可变的字段。
      这样的字段需要通过 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 来标识。
      例如下面的例子中，<lang xml:lang="en-US">ref</lang> 的 val 字段是可变的。
      之后，我们可以通过 <lang xml:lang="en-US">ref</lang> 点 val 来访问这个值，也可以通过 <lang xml:lang="en-US">ref</lang> 点 val 等于 10 来对这个值进行修改。
      <break time="500ms" />
    </p>
    <p>
      我们可以把变量看做是一个放着值的盒子，当我们修改变量的时候，我们是在替换盒子中存放的值。
      例如当我们定义 let <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 为1的时候，我们创建了一个名称为 eks 的盒子，其中存放的值，是1.
      我们执行 eks = 2 这个命令，替换了盒子中的值，为2.
      当这个盒子装着结构体的时候，变量可以看成是指向这个结构体的引用。
      我们用 let 将标识符 <lang xml:lang="en-US">ref</lang> 绑定到一个结构体。
      当我们用 <lang xml:lang="en-US">ref</lang> 修改结构体中的值的时候，我们修改的，是这个结构体中装的可变字段，因此 <lang xml:lang="en-US">ref</lang> 本身并没有发生变化。
      在最后一个例子中，我们定义了一个可变的 <lang xml:lang="en-US">ref</lang> 并对它进行修改，我们其实创建了一个新的盒子，并将 ref 修改<phoneme alphabet="sapi" ph="wei 2">为</phoneme>了指向新的盒子的引用。
      <break time="500ms" /> 
    </p>
    <p>
      那既然是引用，我们当然也可以有多个标识符，指向同一个结构体。
      这种情况我们可以把这些标识符看作是别名。
      例如我们首先定义了一个 alter 函数，它接受两个结构体，并且挨个修改这两个结构体中存放的值。
      之后我们在主程序中定义一个 x，存放了 1。
      我们将 eks 作为参数传给 alter。
      注意，这里的参数 a 和 b，我们都传了 x，因此实际上，x 会发生两次变化，最终变为 20。
      <break time="500ms" />
    </p>
    <p>
      让我们看一下流程图。
      当我们计算 alter eks x 的时候，原本函数中定义的 a 和 b 都被替换<phoneme alphabet="sapi" ph="wei 2">为</phoneme>了 x，因为这是我们传进的参数。
      而我们刚才解释过，当盒子装着结构体的时候，变量是一个指向结构体的引用，因此 a 和 b 实质上是相同的引用，都在指向右下角的结构体。
      之后我们逐个执行指令。
      首先，我们修改原来的 a，于是，结构体的值按照我们的命令发生了变化。
      之后我们修改 b。此时，结构体的值再次发生变化。
      由于它们实质上指向的是同样的结构体，也就是最初 eks 指向的结构体，因此在程序结束之后，x 的值是 20。
      <break time="500ms" />
      以上便是第一部分的内容，下节课我们介绍循环。
      <break time="500ms" />
    </p>
    <p>
      大家好，欢迎来到由IDEA研究院基础软件中心为大家带来的现代编程思想公开课。
      这节课让我们来学习如何使用循环。
      <break time="500ms" />
    </p>
    <p>
      多次执行同一段代码是很常用的。我们曾经使用递归来多次进行同样的计算，现在让我们学习另一种方式，也就是循环。
      月兔提供了三种循环，while, <emphasis level="moderate">for</emphasis 和 loop。
      在月兔中，循环结构也是一个表达式，这意味着，它可以被化简为一个值。
      让我们从最简单的 while 循环开始。
      <break time="500ms" />
    </p>
    <p>
      第一个例子，是将字符串打印3次。我们可以结合变量和 while 循环来做到这一点。
      在这段代码中，我们定义了一个变量 i，它的初始值是0. 
      当 i 小于 3 时，花括号内的代码会被反复执行。
      在导览网站，可以看到，这段代码打印了3次 hello。
      <break time="500ms" />
    </p>
    <p>
      更严谨地看，while 循环包含一个条件判断语句，和一个循环体。
      执行 while 循环时，会首先检查条件表达式是否满足，如果满足，就执行循环体，然后再回到条件表达式的开头；如果不满足，就退出整个循环。
      <break time="500ms" />
    </p>
    <p>
      利用变量，我们可以设定循环条件，在合适的时候跳出循环，以避免循环体无穷无尽地进行，陷入死循环。
      常见的做法是，在进入循环前定义一个变量，针对变量来判断是否要继续循环，然后在循环体的末尾对这个变量进行修改。
      如刚刚的例子，在进入循环前，我们定义了变量 i，在条件判断处，我们借助 i 的值判断循环进行了几次，并且，在循环体的末尾，将 i 的值更新为原来的值 加1.
      这样，我们可以保证，print line hello 这行代码，刚好会执行3次。
      <break time="500ms" />
    </p>
    <p>
      月兔提供了调试器。
      利用调试器，可以更好地观察到程序的运行行为。
      调试器可以逐步执行代码，并实时展示效果。
      在导览网站和本地环境都可以使用调试器。下面我们在本地环境展示调试器的使用。
      例如此处我们在循环前添加断点。
      我们逐步执行语句，可以看到我们首先进行了判定，之后则打印字符串，最后对变量进行修改。
      我们可以在左侧看到我们的数据的变化。
      由于 i 现在是 0，0 小于 3，所以进入循环体。
      第一次进入循环时，打印了一次 hello，i 更新为 1.
      第二次进入循环，再次打印 hello，i 更新为 2. 
      第三次，打印 hello，i 更新为 3. 
      此时，i 小于 3 这个条件不再被满足，因此退出循环，程序结束。
      <break time="500ms" />
    </p>
    <p>
      另外，while 循环的末尾还可以加上一个 else 块。
      当循环条件为假时，else 块的内容会被执行。
      并且，如果 else 块有返回值，这个值即为整个 while 语句的值。
      例如下方的代码，会在打印3次 hello 之后，打印一次 end。
      并且，最终会把 n 绑定为 else 块中变量 i 的值，也就是3.
      <break time="500ms" />
    </p>
    <p>
      循环事实上和递归是等价的。
      对于一个循环，我们可以将它写成递归的形式。
      我们定义一个函数 recurse，它的参数是循环的计数器。
      我们在函数中定义一个条件判断，如果无需继续循环，那么我们直接返回单值类型。
      如果需要继续循环，那么我们执行命令，并且将更新后的参数作为新的参数传进递归调用的 recurse 函数中去。
      而初始值则是在使用 recurse 函数的时候传入。
      例如，这里的两段代码的效果是完全相同的。
      <break time="500ms" />
    </p>
    <p>
      我们有的时候并不希望循环一直进行下去。
      例如，如果我们在查找某个值，当我们找到之后，我们会提前中止循环。
      那么这个时候，我们就有其他的选项来改变我们的循环流。
      break 可以被用来提前中止我们的循环。
      例如在下面的例子中，我们可以跳过从 3 开始的情况。
      continue 则可以被用来跳过当前循环中剩下的内容，在复杂的结构中会比较好用。
      例如在下面的例子中，我们直接跳过了当前计数器为 3 的情况。
      <break time="500ms" />
    </p>
    <p>
      现在，我们来介绍不同于 while 的另一种循环，<emphasis level="moderate">for</emphasis 循环。
      刚刚介绍过，使用 while 循环时，我们经常会用一个变量来判断是否继续循环，并在循环体的末尾对变量进行更新。
      在这种情况下，<emphasis level="moderate">for</emphasis 循环非常方便。
      <emphasis level="moderate">for</emphasis 循环的语法，是在 <emphasis level="moderate">for</emphasis 后面写三个用分号分隔的语句或表达式。
      第一个语句用于定义变量及其初始值，第二个表达式用来判断是否继续循环，第三个语句对变量进行更新。
      然后用花括号包裹需要重复执行的命令。
      上面的 <emphasis level="moderate">for</emphasis 循环，和下面的 while 循环，含义是等价的。
      <break time="500ms" />
    </p>
    <p>
      例如，上面这段代码同样打印三次 hello。
      和 while 循环不同的有两点，第一，定义循环变量 i 的语句，不需要用 let <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang>. 
      第二，循环变量无法在其它的位置进行更改，例如，我们无法在循环体内修改 i 的值。
      因为通常情况下，我们仅仅应该在循环变量更新处，来修改循环变量，这个特性可以防止我们写出错误或者过于复杂的循环。
      <break time="500ms" />
    </p>
    <p>
      结合刚刚介绍的 continue 和 break 指令，<emphasis level="moderate">for</emphasis 循环甚至允许我们不写循环条件和变量更新语句。
      例如此处，在 <emphasis level="moderate">for</emphasis 关键字后面，我们仅声明了循环变量 i 和初始值 0. 
      而在循环体内，我们通过条件判断和 break 指令，在 i 小于 3 不被满足时跳出循环。
      同时，我们引入了一个新的做法，在 continue 指令后面，用一个表达式，表示下一次进入循环时，循环变量的值。
      例如，第一次进入循环时，i 为 0.
      在执行 continue i 加 1 后，再次进入循环，此时 i 的值更新成了 1.
      其实，不仅 continue 可以带上表达式，break 也可以带上表达式，而这些，都是函数式循环的特征。
      <break time="500ms" />
    </p>
    <p>
      什么是函数式循环呢？
      函数式循环既可以重复执行一段代码，但又不会明显地使用变量。
      而是通过灵活使用 break 和 continue 指令来更新循环变量并获得计算结果。
      在 break 后的表达式意味着跳出循环时，会将表达式作为整个循环的返回值。
      在 continue 后的表达式意味着下一次进入循环时，循环变量更新后的值。
      <break time="500ms" />
    </p>
    <p>
      下面我们看一个更复杂的例子。
      这个例子中，我们使用函数式的 <emphasis level="moderate">for</emphasis 循环来计算阶乘。
      在代码的第二行，我们定义了两个循环变量，i 和 <lang xml:lang="en-US">res</lang>。
      并分别为它们赋予初始值，n 和 1.
      其中，i 代表我们正在处理的数字，它会从 n 开始，反复减 1，直到为0. 
      而在这个过程中，我们不断更新计算的结果，每经历依次循环，计算结果 <lang xml:lang="en-US">res</lang> 就会乘上 i。
      当 i 等于 0 时，意味着我们的阶乘计算已经结束，这时使用 break <lang xml:lang="en-US">res</lang> 来退出循环，并把 <lang xml:lang="en-US">res</lang> 当做循环的返回值。
      由于这个循环是 fact 函数内唯一的表达式，所以 <lang xml:lang="en-US">res</lang> 同时也是 fact 函数的返回值。
      在测试代码中，我们验证 5 的阶乘，为一百二十。
      <break time="500ms" />
    </p>
    <p>
      当我们在循环体内，需要对循环变量进行模式匹配时，可以使用 loop 循环。
      loop 循环无需定义循环变量，只需要定义初始值。在循环体内，直接对循环变量进行模式匹配。
      它相当于用 <emphasis level="moderate">for</emphasis 循环定义一个循环变量，并且循环体内只有一个模式匹配语句，匹配的对象刚好也是循环变量。
      <break time="500ms" />
    </p>
    <p>
      仍然是阶乘函数的例子，这一次，我们使用 loop 循环来实现。
      我们把两个循环变量打包成一个二元组，分别赋予初始值 n 和 1. 
      接下来，当第一个循环变量匹配到 0 时，跳出循环，并返回第二个循环变量作为结果。
      否则，进入下一轮循环，并把循环变量更新为 i 减 1 和 <lang xml:lang="en-US">res</lang> 乘 i。
      <break time="500ms" />
    </p>
    <p>
      关于循环的介绍就到这里了，最后，我们提一下月兔提供的代码检查。
      如果我们声明了一个变量，那么月兔会检查它是否被修改过。
      一种场景就是在写循环的时候可能会忘记对计数器进行更新。
      这种时候，我们就可以发现错误，尽早修正。
      月兔还会检查我们的返回值是否和类型声明相同，即便我们的返回类型声明的是单值类型，也不会自动舍弃运算结果，而是需要我们严格地保证函数返回值的类型和声明的类型一致。
      这也是为了避免我们可能会写错函数返回类型。
      <break time="500ms" />
    </p>
    <p>
      可变数据虽然在理解计算模型上会比函数式的替换化简更加困难，并且可能会引入很多潜在的问题，但是应用场景更广泛。
      举例来说，我们直接操作程序外环境，例如硬件的时候，就很可能用到可变数据。
      可变数据在一些时候性能会更好，例如在随机访问数据的时候，数组这一数据结构会比我们一直使用的列表性能更好。
      可变数据允许我们构建某些复杂的数据结构。
      而可变数据的原地修改也可以让我们更好地利用内存空间，因为原地修改不会引入额外的空间消耗。
      当然，为了排除可变数据带来的负面影响，我们应当优先考虑使用不可变数据，或者使用函数式循环，来代替普通的循环。
      <break time="500ms" />
    </p>
    <p>
      同时，可变数据也并不总是与引用透明性冲突。
      例如在这里，我们定义了一个简单的利用了可变数据的斐波那契数列。
      我们利用动态规划，从 0 开始向上更新计算我们所寻找的项数。
      在这个过程中，和递归版本不同的是，我们通过循环和修改变量来进行数据的更新。
      但不论如何，对于各处使用了 fib <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 进行计算的地方，我们可以直接把 fib <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 给替换为最终的计算结果，例如 fib <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="mjuːt">mut</phoneme></lang> 1 可以替换为 1，而运行的结果都是相同的，因为我们并没有产生任何的副作用。
      <break time="500ms" />
    </p>
    <p>
      总结，本章节初步接触了命令式编程，了解了如何使用一些命令，如何使用变量，以及如何使用循环等。
      以上便是本节课的全部内容，感谢大家的观看。
      <break time="500ms" />
    </p>
  </voice>
</speak>
