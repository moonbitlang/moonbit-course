<speak version="1.0" xmlns="http://www.w3.org/2001/10/synthesis" xml:lang="zh-CN">
  <voice name="zh-CN-YunyiMultilingualNeural">
    <p>
      大家好，欢迎来到由IDEA研究院基础软件中心为大家带来的现代编程思想公开课。
      今天让我们来学习泛型、高阶函数和迭代器。
      <break time="500ms" />
    </p>
    <p>
      我们在进行程序设计的时候会遇到相似的数据结构，相似的运算。
      在这种时候，我们可以通过建立良好的抽象来对这些信息进行重复利用，既可以保证可维护性，也可以让我们忽略一些细节。
      良好的抽象应当具备两个条件：
      代码中重复多次出现相同的模式或结构，并且抽出的逻辑是具有合适的语义。
      例如，对于一个整数的列表进行求和就是一个很好的例子。我们在很多场合都可能会需要对整数列表进行求和运算，因此会有多次重复。而求和这一操作具有合适的语义，因此适合被进行抽象。我们可以把这个运算抽象到一个函数。
      编程语言在这个过程中则为我们提供了各种抽象的手段，例如函数、泛型、高阶函数以及接口等。
      今天的课程中，我们将会介绍泛型和高阶函数，以及大量使用了高阶函数的迭代器。
      <break time="500ms" />
    </p>
    <p>
      我们首先以堆栈这个数据结构为例，来看一下我们为什么会用到泛型，以及怎么使用泛型。
			<break time="500ms" />
      堆栈是一个由一系列对象组成的一个集合，这些对象的插入和删除遵循后进先出原则，也就是Last In First Out，一般缩写为 Lyefo。
      例如左下图中的船上码放的集装箱，显然我们从上面把新的集装箱堆叠上去，
      而当我们取走集装箱的时候，我们也是从上面取走，也就是最后放入的集装箱最先被取走。
      右侧的石堆一样，如果不想打翻这个石堆，那么我们只能从上面添加石头，或者将最近添加的石头移走。
      这样的结构就是堆栈。我们的生活中还有很多很多的例子，在此就不一一枚举。
      <break time="500ms" />
    </p>
    <p>
      对于数据类型的堆栈，我们可以定义如下的操作。
      以存储整数的堆栈 IntStack 为例，我们可以创建一个新的空的堆栈；
      我们可以往堆栈中添加一个整数；我们可以尝试试图从堆栈中取走一个元素，因为堆栈可能为空，因此这个元素可能有也可能没有，用Option包裹。
      如下图所示，我们添加一个2，之后取走一个2。我们简单的实现这个堆栈的定义。
			<break time="500ms" />
      在代码示例中，我们看到，我们在函数名字前添加了 IntStack 和两个冒号，这意味着该函数是 IntStack 类型的方法。
			当方法的第一个参数的类型刚好也是 IntStack 时，可以使用“点语法”来调用方法。
			从而，我们可以用多个“点语法”实现链式调用。
			比如，我们可以写 empty 点 push 点 pop，而不是 pop 括号 push 括号 empty。这种写法的更深层的含义，我们在后续课程会介绍。
      回到我们的代码，我们根据堆栈的操作定义了一个递归的数据结构：
      一个堆栈可能为空，也可能为一个元素和一个堆栈。
      创建堆栈即是创建一个空堆栈，而添加元素，则是构建一个非空的堆栈，其中栈顶元素是我们要添加的元素，而底下的堆栈则是原来的堆栈。
      从堆栈取出元素，则需要通过模式匹配，若模式为空，则没有数值可取；
      若模式非空，则可以取一个元素。
      我们可以看到堆栈的定义和列表的定义十分类似。
      <break time="500ms" />
    </p>
    <p>
      在定义了整数的堆栈后，我们也会想定义其他类型的堆栈，例如字符串的堆栈。
      这很简单，我们在此仅仅展示代码，不作解释。
      仔细观察便会发现，字符串的堆栈和整数的堆栈长得一模一样，除了一些类型的定义不同。
      而如果我们想要添加更多的数据类型，那难道我们要对每一个数据类型都重新定义一个堆栈的数据结构吗？这显然是不能接受的。
      <break time="500ms" />
    </p>
    <p>
      因此月兔提供了泛型这一语言特性。
      泛型也就是将类型作为参数，让我们能定义更加抽象、更加可服用的数据结构与函数。
      例如我们这里的堆栈，我们可以在名称后面加上一个类型参数 T 来表示它实际存储的数据类型。
      之后定义的函数也是同样，在函数声明的 fn 关键字后我们会看到一个 T 作为类型参数，
      代表稍后操作的堆栈中存储的数据类型以及我们想要添加的数据的类型。
      我们只需要像计算函数时将标识符替换为参数一样，将 T 替换为具体的类型，便可以得到实际的数据结构和函数。
      例如，这里的 T 如果替换为 Int ，那我们获得的就是先前定义的 IntStack。
      <break time="500ms" />
    </p>
    <p>
      语法我们刚才介绍过了，这里可以看看更多的例子。
      例如，我们可以定义<phoneme alphabet="sapi" ph="yi 2">一</phoneme>对数据，也就是二元组。
      二元组的两个元素不必是相同类型，因此我们的类型参数也有两个。在 Pair 中存储的值 first 和 second 分别是这两个类型。
      之后我们定义一个函数，它可以对<phoneme alphabet="sapi" ph="ren 4 yi 4">任意</phoneme>类型进行运算，总是返回输入值。
      Stack 和 Pair 本身可以被看成是类型上的函数，它们的参数是 T，
      运算的结果则是 Stack T 和 Pair A B 这些具体的类型，因此它们可以被看成是类型构造器。
      类型参数在多数时候都可以被月兔根据具体的参数类型推导。
      例如这里的截图中，empty 的类型最初是未知的。
      但是在 push 1 之后，我们明白，它是被用来装整数的，因此，我们可以推出 push 以及 empty 的类型参数应当是整型 Int。
      <break time="500ms" />
    </p>
    <p>
      现在让我们来看另一个泛型数据结构：队列。
      我们上节课已经在广度优先排序的时候用到了队列。
      回忆一下，队列是一个先进先出的数据结构，就像我们日常排队一样。
      我们在这里定义如下的操作，其中队列叫做 Queue，它有一个类型参数。
      因此，所有的操作也都有一个类型参数，来表明它所拥有的数据的类型。
      我们定义了三个和堆栈类似的操作，唯一的区别是，在取出元素的时候，应当取出最先加入队列的元素。
      队列的实现可以通过一个列表，或者堆栈模拟，我们添加的时候将元素加在列表最后，
      也就是堆栈底部，取的时候从列表最前取，也就是堆栈顶部。
      取的操作十分快捷，因为只需要进行一次模式匹配即可。
      但是添加元素的操作却需要重新构建整个列表。
      像我们现在所看到的，头部为2的列表，其尾部本来是一个空列表，
      现在却要被替换为一个包含2、3的列表；
      同样地，头部为1的列表，其尾部本来是只包含2的列表，现在却要被替换为包含2、3的列表。
      也就是说，整个列表从头到尾都要被重新构建一番，耗时与整个列表长度成正比。效率十分低下。
      <break time="500ms" />
    </p>
    <p>
      我们可以使用两个堆栈来模拟队列。
      其中一个堆栈负责取出操作，另一个堆栈负责存储操作。
      可以看到，在定义中的两个类型都是 Stack T，而 T 则来自整个队列的类型参数。
      我们在添加数据的时候，直接存入 back：这一步通过在原来的数据结构上构建一个新的数据结构，因此速度很快；
      而取出操作也只需要进行一次模式匹配，同样不慢。
      而当 front 中的元素被取完，此时我们需要把 back 中所有的元素通过一次旋转放入 front中。
      我们在每次操作后进行这个检查，保证只要队列非空，则 front 非空，这是我们队列操作的不变式，也就是必须成立的条件。
      这一次的旋转的开销非常大，与当前的列表长度成正比，
      但是好消息是，这一次开销可以被平摊出去，因为一次旋转之后，接下来的多次取出操作都不再需要进行旋转。
      <break time="500ms" />
    </p>
    <p>
      我们来看一下具体的例子。
      在下方，我们一开始拥有一个空的队列，因此两个堆栈均为空。
      在进行了一次添加操作之后，我们把一个数字添加到 back 中去。
      此时我们对队列进行整理，发现队列不为空，但 front 为空，这不符合我们之前所说的不变式，
      因此我们对 back 中的堆栈进行一次反转操作之后放到 front 中去。
      之后，我们再次进行操作，则继续向 back 中添加元素。
      因为此时 front 不为空，因此符合不变式，我们也不会额外进行处理。
      <break time="500ms" />
    </p>
    <p>
      之后，我们反复进行添加，都只是在 back 中进行快速的添加创建新元素这一操作。
      然后，我们从 front 中取出元素。
      我们一样在操作后检查不变式是否被打破。
      我们发现，队列非空，而 front 为空，因此我们对 back 中的堆栈再次进行反转之后放到 front 中去。
      之后，我们便可以正常地从 front 中取元素了。
      可以看到，一次反转之后便可支持多次操作，因此总体开销相比每一次都重新构建列表而言是小很多的。
      <break time="500ms" />
    </p>
    <p>
      现在大家看到的就是队列的代码。
      可以看到，我们大量应用了泛型，因此我们的队列可以放任何的类型，包括装有其他元素的队列。
      我们这里看到的函数都是对我们刚才解释的算法的具体实现。
      <break time="500ms" />
    </p>
    <p>
      以上便是本节课的内容。这节课，我们学习了泛型。
      下节课，我们将学习高阶函数。
      <break time="500ms" />
    </p>
    <p>
      大家好，欢迎来到由IDEA研究院基础软件中心为大家带来的现代编程思想公开课。
      在上节课，我们介绍了泛型。
      接下来，我们学习高阶函数。
      这个部分依然是围绕着如何利用月兔提供给我们的功能来减少重复代码、提高代码的复用，让我们从一个例子开始看起。
      <break time="500ms" />
    </p>
    <p>
      我们会有一些列表操作。
      例如，求一个整数列表的和，我们利用结构化递归写出如下代码：若为空，则和为0；若非空，则和为当前值加剩余列表的元素求和。
      又比如说，求一个任意数据类型的列表的长度。同样利用结构化递归，我们可以写出如下代码：若为空，则长度为0；若非空，则长度为1加上剩下的列表的长度。
			<break time="500ms" />
    </p>
    <p>
      我们仔细观察会发现这两个结构具有着相当的相似性：都是结构化递归，
      在为空的时候有一个默认值，而非空的时候，则都是对当前值进行一个处理后，与剩余列表的递归结果进行一个运算。
      例如，在求和的例子中，默认值是0，而二元运算则是相加。
      在求长度的例子中，默认值也是0，而二元运算则是将当前值替换为1，然后与剩下的结果相加。那么如何重复使用这个结构呢？我们可以写成一个函数，将这个默认值以及这个二元运算作为参数传进来。
      <break time="500ms" />
    </p>
    <p>
      这就不得不提到，在月兔中，函数是一等公民。
      这就意味着，我们既可以把函数作为参数传递，我们也可以将函数作为计算结果存储。
      接受函数的函数，或返回函数的函数叫作高阶函数。如下的 fold right 函数就是一个高阶函数。
      其中我们将 f 这个函数作为参数传进来，并且在第四行使用这个函数进行计算。
      <break time="500ms" />
    </p>
    <p>
      我们再看另一个例子。
      如果我们想要重复一个函数的运算，那么我们可以定义如第一行所示的 repeat。
      repeat 接受一个函数作为参数，之后返回一个函数作为结果。
      它的运算结果就是会将原函数计算两次的函数。
      例如，我们有一个加一的函数，和一个加二的函数。
      我们如果使用 repeat 并将加一的函数作为参数传入，那么作为结果，我们会获得一个加一两次，也就是加二的函数。
      我们用 let 来绑定这一函数到 add two 上，之后用正常的函数计算的语法进行运算，获得结果。
      <break time="500ms" />
    </p>
    <p>
      我们来看一下高阶函数的化简。
      首先是 add two，绑定的是 repeat plus one。
      在化简的时候，我们像正常的函数一样，用参数去替换表达式中的标识符，于是获得了一个函数作为结果。
      到这一步，我们无法继续化简。
      接着我们来看应用 add two 进行计算。
      那么同样地，我们替换表达式中的标识符，对 plus one 进行化简。
      之后，我们再次化简一次，并最终获得了我们的结果，4
      <break time="500ms" />
    </p>
    <p>
      函数的类型我们之前提过，是从接受的参数到输出参数，其中接受的参数是用括号包裹的。例如，整数到整数，那就是 Int to Int。
      而第二行则是整数到函数的例子。
      注意到，这里的函数的参数也要用括号包裹来表示。
      这个函数的类型实际上等同于是将后面的整个函数类型用括号包裹，如第三行。
      如果是从函数到整数，那么我们刚才提过，接受的参数是要用括号包裹的，因此它应该看起来像第四行那样，而不是像第二行。
      <break time="500ms" />
    </p>
    <p>
      我们在这里再来看几个常见的高阶函数的应用。
      我们刚才看到的 fold right 就是其中的一个常见的高阶函数。
      我们在下方画出它的表达式树。可以看到，对于一个从1到3的列表，我们每次都是将 f 应用到当前元素以及剩下的元素的运算结果上，
      因此看上去，我们是在用 b 从右往左一个个构建折叠起来最终获得一个结果，因此它被称为 fold right。
			<break time="500ms" />
      那么如果我们换个方向，将列表从左侧向右侧折叠，那么我们获得的便应该是 fold left。
      这里，我们只需要调换顺序，先将当前元素与之前积累的结果进行一个处理，然后再将处理的结果放入后续的处理当中，如大家第四行所见。
      这样的话便是从左向右折叠。
      <break time="500ms" />
    </p>
    <p>
      另一个常见的高阶函数的应用是将函数中的元素进行一一映射。
      例如，如果我们有一些人的信息，而我们只需要他们的名字，
      那么我们就可以通过 map 这个映射函数，接受 f 这个参数，来对列表中的元素进行一一映射，
      最后获得一个新的列表，而这个列表中的元素类型则变成了 B。
      这个函数的实现十分简单，同样只需要结构化递归即可。
      最后的应用则是如第8行所示。
			<break time="500ms" />
			不知道大家有没有一种感觉，这个 map 的结构好像在哪里看到过。它的结构是结构化递归，空值有一个对应的默认值，非空时对当前值进行处理后和递归结果进行二元运算。
      事实上，完全可以通过 fold right 来实现 map，其中默认值是空列表，而二元运算则是Cons 这个构造器。
      我们在这里给大家留一个思考题，如何用 fold right 来实现 fold left 呢？一个小提示是，可能会有一个概念叫做 Continuation，
      它代表着计算出当前值之后剩余的运算，一般是一个函数，参数是当前值，返回值则是整个程序的运算结果。
      <break time="500ms" />
    </p>
    <p>
      在学习了泛型和高阶函数之后，我们便可以将上节课学习的二叉搜索树定义为更一般的二叉搜索树，可以存储各种数据类型，而不仅仅是整数。
      其中，数据结构本身接受一个类型参数，来代表它所存储的数据类型。
      而考虑到二叉搜索树应当是有序的，那么我们需要知道如何排列这个特定的类型，
      因此我们接受一个比较函数作为参数，它应当返回一个整数，以负数、0和整数来分别代表比较的结果，是小于、等于还是大于，如代码所示。
      事实上，我们完全可以利用月兔的另一个语言特性来省略这一个参数。我们将会在后续课程进行介绍。
      <break time="500ms" />
    </p>
    <p>
      以上便是本节课的内容。
      下节课，我们将学习迭代器。
      <break time="500ms" />
    </p>
    <p>
      大家好，欢迎来到由IDEA研究院基础软件中心为大家带来的现代编程思想公开课。
      在上节课，我们介绍高阶函数。
      这节课，我们来学习一个强大的工具，迭代器，它的实现充分利用了我们前面学过的泛型和高阶函数。
      <break time="500ms" />
    </p>
    <p>
      在很多场景下，我们希望对数据进行遍历。
      比如列表、树、队列，甚至网络流，都可以看作是“一个接一个的元素”。
      我们已经学习了如何为这些“数据结构”实现映射和折叠等操作，
      那么为什么需要用迭代器呢？原因在于，迭代器提供了统一的数据遍历接口，在使用迭代器时，不需要关心底层“数据结构”。
      有了迭代器，这些类型都可以转换成迭代器。使用迭代器时也无需关心背后存储数据的具体类型是什么。
      此外，迭代器有一个很大的好处——其构造过程是惰性的。构造迭代器的时候，不会立即访问数据，也不需要立即为中间值分配内存，甚至可以按需生成无穷的数据。
      当需要使用无穷多的数据，或者不确定未来有哪些数据会被访问时，迭代器可以节省大量的空间和时间。
      <break time="500ms" />
    </p>
		<p>
			迭代器有两种截然不同的设计思路，内部迭代器，和，外部迭代器。
			内部迭代器由调用者提供回吊函数，迭代过程由迭代器自身来执行，且迭代过程一旦开始，就会一直进行到结束，无法在中途停下来。
			内部迭代器的典型用法如上面这行代码所示，一个迭代器对象调用 each 方法，并传入一个回吊函数，随后迭代器中的每个元素都会被这个回吊函数访问。
			<break time="500ms" />
			外部迭代器则是由调用者主动从迭代器一个个拉取元素，这也意味着，只要调用者停止拉取元素，迭代过程就可以随时终止。
			外部迭代器的典型用法，是使用一个 while 循环语句，不断地调用迭代器的 next 方法，当返回值是一个 some 时，访问元素。
			关于 while 循环，后续课程会详细解释。
			在今天的课程中，我们介绍内部迭代器。
			<break time="500ms" />
		</p>
    <p>
      下面这个例子展示了迭代器的强大威力。
      第二行定义了一个用于生成所有自然数的迭代器 naturals。暂时忽略这个复杂的定义过程。
      我们借助 naturals 这个神奇的迭代器，利用一系列转换操作，计算大于 100 的前十个完全平方数。
      在第11行，我们计算每个自然数的平方。
      然后，在第12行，我们筛选出大于 100 的结果；
      最后，在第13行，使用 take 取得前 10 个结果。
      这样，就计算出了大于 100 的前十个完全平方数。
      可以看到，迭代器非常适合用于处理需要遍历的数据，
      并且，每一层操作都不会立即遍历所有数据，而是像水管一样一层层包裹起来，
      只有最终消费的时候，才会触发整个流水线。
      <break time="500ms" />
    </p>
    <p>
      那么迭代器究竟是什么呢？
      迭代器是一个对象，用于遍历序列中的每一项，并决定何时结束遍历。
      在月兔中，迭代器是包含一个类型参数 T 的类型构造器，<lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>。
      它用于遍历元素类型为 T 的序列。
      举个例子，对于一个包含数字1、2、3的列表，list一二三，假设列表有一个方法 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>，可以生成整数类型的迭代器，
      那么，对这个迭代器调用 each 方法，并传入一个打印每一项元素的函数，print line，我们就会看到，输出为 1、2、3.
      print line 会打印数字并加上一个换行符，这个命令将在下节课详细介绍。
      在这里，我们已经可以看出，迭代器拥有一个 each 方法，可以让我们按序访问其中的元素。这是最常见的消费迭代器的方式。
      <break time="500ms" />
    </p>
    <p>
      除了 each，我们还有很多其它的消费迭代器的方式，如上面的这些方法。
      其中，count 用来计数迭代器中元素的数量；
      <lang xml:lang="en-US">head</lang> 用来取迭代器的第一个元素；因为迭代器可能为空，所以 <lang xml:lang="en-US">head</lang> 的返回值是一个 Option。
      <phoneme alphabet="ipa" ph="ɛnθ">nth</phoneme> 用来取第 n 个元素，返回值同样是 Option。
      仍然以 list一二三的迭代器为例，它的元素个数是3，头部元素是1，第一个元素是2.
      <break time="500ms" />
    </p>
    <p>
      迭代器的常见用法，是基于已有的迭代器，经过多轮转换，最终消费，得到需要的结果。
      其中，每一轮转换都会生成一个新的迭代器。例如，map 方法会把原来迭代器的每一个元素映射成一个新的元素，就像前面介绍过的列表上的映射一样。
      filter 方法过滤迭代器中的部分元素，只有谓词 p 调用结果为 true 的元素会保留下来。
      take 会取出迭代器中的前 n 个元素，构成一个新的迭代器。
      concat 连接两个迭代器，构成一个新的迭代器，新的迭代器会先迭代第一个迭代器中的元素，再迭代第二个迭代器中的元素。
      以 map 为例，下面的例子中，list一二三的迭代器被 map 方法转换为了一个新的迭代器，转换过程是把每个数字映射成比它大1的数字。
      可以看到，转换后的迭代器包含元素2、3、4，这里迭代器默认以方括号包裹、逗号分隔的数组形式展现。
      <break time="500ms" />
    </p>
    <p>
      接下来我们要进入迭代器的内部，看看迭代器到底是如何实现的。
      本质上，迭代器是一个高阶函数。
      它接受一个函数作为参数，这个参数通常命名为 yield；
      它代表的是元素的消费者。
      yield 会返回一个 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Result，迭代器自身也会返回一个 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Result。
      <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Result 代表迭代器继续迭代元素还是结束。
      在迭代器的类型定义中，第一个 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Result 代表消费者是否继续消费元素，第二个 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Result 则代表当前迭代器自身是否要继续迭代。
      迭代器在运行时，需要将元素一个个喂给 yield 函数。当 yield 返回 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>End 或者已经没有更多元素可以访问时，意味着迭代已经结束。
      <break time="500ms" />
    </p>
    <p>
      以列表迭代器的实现过程为例。
      第12行使用 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang> new 函数，使用一个匿名函数生成迭代器。这个匿名函数的核心是递归的 go 函数。
      go 函数展现了迭代访问一个列表的大致过程。首先对列表进行模式匹配，当列表为空时，没有元素需要继续访问了，直接返回即可；
      当列表不为空时，头部元素和后续列表分别被绑定到 <lang xml:lang="en-US">head</lang> 和 <lang xml:lang="en-US">tail</lang> 两个标识符；
      此时，使用 yield 函数访问头部元素，并根据访问结果决定继续迭代尾部列表还是中止访问。
			第六行使用了一个守卫语句，来检查 yield 函数访问头部元素的结果是否为 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Continue，如果条件成立，就继续执行后续语句；如果不成立，就会执行 else 块中的代码，一个 return 语句，提前从函数中返回 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>End，中止后续访问。
      什么时候会提前中止迭代呢？例如，前面提到的 take 方法，当访问了 n 项后，其余的项不再需要被访问，此时 yield 就会返回 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>End，迭代器就应当停止。
      <break time="500ms" />
    </p>
    <p>
      再看前面提到的例子。
      这个例子中，我们用迭代器实现了一个计数器 naturals，它可以从0开始生成无穷无尽的自然数。
      它的实现过程和列表迭代器是类似的，只不过需要迭代的内容从列表元素变为了整数。
      在测试代码中，我们可以看到，naturals 的第0、第10、第100项分别为0、10、100.
      你可能会好奇，naturals 是怎么把无穷多的自然数，保存在迭代器中的呢？
      事实上，这正是先前提到的，迭代器的构造过程是惰性的。
      只有当第四行的 yield 函数执行，并返回 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Continue 时，go 函数才会继续递归。
      因此，迭代器并没有保存任何元素，而是在需要时逐个生成元素。因此既不需要无穷的空间来保存，也不需要无限的时间来计算。
      <break time="500ms" />
    </p>
    <p>
      现在，让我们回到前面获取大于100的前十个完全平方数的例子。
			在代码的第11、12、13行，分别使用 naturals 创建了3个中间迭代器。
			最终，在 <lang xml:lang="en-US">inspect</lang> 函数内，迭代器 n 被调用，对于每个元素，都会调用 write object 函数把它们写入到一个字符串中。
			<break time="500ms" />
		</p>
		<p>
			迭代器的调用链如图所示。
			<break time="1000ms" />
			接下来，通过探究第0、11、20次迭代的过程，来深刻理解迭代器的工作流程。
			<break time="1000ms" />
		</p>
		<p>
      在第0次迭代时，naturals 迭代器调用了 yield 0，这个 yield 即为下游迭代器 map 迭代器的处理过程。
      0 的平方是 0，map 继续调用 yield 0。
      filter 迭代器判断发现0不大于100，因此直接返回了<lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Continue，而没有继续调用 yield.
			map 迭代器也同样返回 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>Continue，至此，第0轮迭代结束，naturals 的控制逻辑启动第1轮迭代。
      <break time="500ms" />
    </p>
    <p>
      在第11次迭代时，naturals 迭代器调用了 yield 11；
      11 的平方是 121，map 迭代器调用 yield 121。
      此时，因为 121 大于 100，所以 filter 迭代器调用了 yield 121.
      最后，take 迭代器调用了 yield 121，这意味着 n 的第 0 个元素是 121. 同时，take 迭代器内的计数器自增了1.
      <break time="500ms" />
    </p>
    <p>
      在第20次迭代时，naturals 迭代器调用了 yield 二十；
      map 迭代器调用 yield 四百。
      filter 迭代器同样调用 yield 四百.
      take 调用 yield 四百 后，迭代器内部的计数器已经达到了上限——10，因此返回了<lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>End。
      这意味着迭代过程结束。
			迭代器的定义中，下游迭代器结束迭代时，自身也会结束。
      因此，所有的上游迭代器最终都返回了 <lang xml:lang="en-US"><phoneme alphabet="ipa" ph="ˈɪtə">Iter</phoneme></lang>End。
			整个迭代过程也就此结束。
      最终，400 就成了 n 的最后一个元素。
      <break time="500ms" />
    </p>
    <p>
      最后总结一下。今天的课程中，我们学习了泛型、高阶函数和迭代器，
      看到了它们在月兔中是如何实现和应用的。
      我们推荐大家阅读 <lang xml:lang="en-US">Software Foundations</lang> 第四章或者 <lang xml:lang="en-US">Programming Language Foundations in Agda</lang> 第十章。
      我们下节课再见。
    </p>
  </voice>
</speak>
          